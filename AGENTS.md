# AGENTS.md

## 工程原则

- 以长期可维护性和完整交付为目标。重构应覆盖完整影响面并删除废弃路径；没有明确且已验证的兼容需求时，不保留兼容层、双实现或兜底分支。
- 包和目录按功能域与职责组织，避免平铺文件，也避免没有独立职责的形式化分层。
- 评审和修改必须沿依赖关系检查全部受影响模块，不能只处理应用层或最显眼的调用点。
- 设备兼容性按协议入口与能力建模；已测试型号只限定验证结论，不构成封闭的支持列表。通用 UI、业务和接口不得硬编码当前实机型号，型号特有行为必须隔离并标明证据。
- 新增依赖、抽象、功能或测试前先做消融判断：移除后如果当前功能、风险控制、可测试性或维护成本没有实质恶化，就不应引入。

## 跨平台边界

- 采用 shared-first：凡不依赖真实平台能力的 UI、状态、业务规则、工作流和接口都应位于共享代码中，不能因当前目标恰好兼容某个平台 API 就下沉到平台 source set。
- 平台层只负责平台能力及其初始化，共享层通过公共接口和依赖注入使用它们。中间 source set 与 `expect`/`actual` 只用于确实存在的平台差异，不承载可共享业务逻辑。
- 不为表面上的统一强行选择同一种平台实现；网络引擎、系统集成等按目标平台选择，配置和业务请求保持共享。
- 公共代码优先使用 Kotlin 标准库和成熟的 KMP API，不为假设中的未来需求提前增加基础依赖或自行维护平行基础设施。
- API Key 保存在应用私有的 DataStore 即可，不增加自定义加密、平台密钥系统或兼容迁移逻辑；任何日志和诊断信息都不得泄露密钥。

## 文档边界

- 同一事实只维护一个权威来源，其他位置通过引用复用，禁止复制结论导致多处同步。
- `docs/architecture.md` 维护 Hanppie 当前实现、工作原理、能力和安全边界；`docs/architecture-robomaster.md` 维护 RoboMaster 原生架构、协议、调查和上游结论。相关事实变化时同步更新对应文件。
- README 只维护安装和用法；单次证据进入日期化记录或 `diag` 报告；废弃方案、迁移过程和历史取舍留在记录或 Git 历史中。
- 文档必须明确区分当前事实、未验证边界和未来计划，不能用模糊表述混写。

## 上游、运行时与安全

- 不随意修改恢复内容或上游 fork；必要修改必须保留导入接口、许可证、版权和来源记录，并由测试、报文或实机证据支持。
- App/Lab 协议修改必须有报文或真机证据；机内载荷保持 Python 3.6 兼容、显式 arm 和失联归零，主机代码与 CI 以 Python 3.10 为基线。
- 不提交设备备份、序列号、凭据、厂商二进制或自动生成的实机日志。

## 体验、验证与交付

- UI 改动后以产品和交互视角审查信息层级、状态反馈、错误恢复、可发现性以及手机、平板、桌面上的适配效果。
- 测试应保护真实契约、风险和回归面。静态检查、自动化测试、平台构建、UI 运行、设备连接与物理动作属于不同证据层级，不能互相替代。
- 提交前运行 `task check` 和 `task prek`，并按影响面补充平台构建、lint、UI 或实机验证；无法完成的层级必须明确说明边界。
- 只在用户明确要求时提交或推送。提交应限定到任务相关路径，保留无关改动；提交消息遵循约定式提交规范，使用中文说明实现、影响和验证结果。
