# 首页三维场景验证（2026-09-22）

当前实现与安全边界见 [架构](architecture.md#首页三维展示)，素材的权威来源与生成方法见
[机器人展示素材](../assets/robot-scene/README.md)。本记录只描述本次验证，不作为实机能力声明。

## 环境与结果

运行环境为 macOS arm64（Apple M5 Pro）、JDK 25，以及 Android 17 的 Medium_Phone arm64 模拟器。

| 检查层级 | 命令或方法 | 结果 |
| --- | --- | --- |
| 协议与会话 | `./gradlew :packages:robot-core:desktopTest` | 60 项，55 通过、5 项实机等可选测试跳过；建立会话订阅云台，不发送云台/底盘速度命令 |
| 共享状态与界面 | `./gradlew :shared:jvmTest` | 254 项，246 通过、8 项可选测试跳过；包括姿态接收时间、过期/非法帧、断线、组件外观映射与现有界面回归 |
| 桌面真实 GPU 与恢复 | `HANPPIE_GPU_TESTS=1 ./gradlew :shared:jvmTest --tests '*RobotSceneUiTest' --tests '*SceneRecoveryUiTest'` | 5 项通过；真实 GPU 4 项，注入加载失败后的重试 1 项 |
| Android 生命周期 | `./gradlew :androidApp:connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=cn.elonzh.hanppie.HomeSceneUiTest` | 1 项通过，覆盖页面切换、视角操作、后台恢复和横屏布局 |
| Android 构建及静态检查 | `./gradlew :androidApp:assembleDebug :androidApp:lintDebug` | 通过 |
| 桌面分发包 | `./gradlew :desktopApp:createDistributable`，打开本工作树生成的 Hanppie.app | 通过，包内运行时正常加载原生库并显示模型 |
| 素材格式 | Khronos glTF Validator 检查三个 GLB | 均为 0 error、0 warning；分别约 353、380、379 KiB |
| 素材生成脚本 | Ruff 检查/格式、Pyright（安装生成器依赖） | 通过 |
| 改动格式 | `git diff --check` | 通过 |

桌面 GPU 测试比较实际图像：改变 yaw/pitch 后几何像素显著变化；旋转观察视角后，重置恢复同一
视角且不改变遥测姿态。并检查浅色桌面、深色平板、360dp 英文大字体、机械臂和基础底盘外观。
截图输出到 `shared/build/reports/ui/`，不提交自动生成截图。

Android Compose 测试暂停自动推进的测试时钟，避免持续渲染循环无法进入 idle；其通过仅证明
原生资源初始化和界面/生命周期契约，不证明合成器中的最终像素。另以正常运行的 APK 检查了
竖屏、横屏以及从系统主页返回后的屏幕截图，确认模型、阴影和连接操作正常显示。

## 验证边界

- 本次没有连接真实机器人。普通会话订阅能否持续收到设备云台推送，以及角度正负方向/全行程
  与真实转轴的对应关系，仍需实机验证。
- 图像测试通过输入遥测姿态验证渲染联动；不证明机器人发生了物理动作。
- 未校准底盘姿态与世界轨迹，不模拟轮子运动；机械臂外观不代表已接入关节遥测。
- 尚未运行 Windows、Linux 桌面包或 Android 实体设备，也未测定这些平台的帧率、功耗和内存上限。
- 构建仍有项目已有的 Kotlin beta/deprecation 和 Gradle 10 兼容性提示；本次没有扩大范围清理它们。

## 同日视觉重做复核

以上素材大小与首轮画面属于初版；当前版本已替换为上游 RoboMaster 网格和装配关系，
不再使用自制几何简模。当前来源、许可证和转换过程以素材文档为准。

- 去掉独立展示卡片和着色地面，采用透明合成、页面接触阴影与补光。模型、连接操作和状态位于同一页面表面。
- 替换三种外观后，真实 GPU 的 4 项画面/姿态测试及 1 项失败重试测试通过；截图重新人工检查。
- 共享测试首次发现次要连接操作触控高度从 52dp 缩到 48dp，已恢复 52dp，并通过整个 `ConsoleUiTest` 回归。
- Android 构建、lint 和桌面分发包生成通过。Android 生命周期测试通过，并以正常运行的 APK 截图确认新网格与透明背景正确显示。模型转换脚本通过 Ruff 和 Pyright。
- 三个新 GLB 均无格式错误；带法线贴图的 S1/EP 各有 3 条运行时生成切线空间的可移植性提示。
  这不作为跨平台图像一致性的保证，仍需按实际后端检查画面。

## 同日贴图与场景补全复核

本节对应最终的贴图版本；前两轮的无贴图材质、二维阴影及素材大小不代表当前结果。

- 按原始材质记录恢复装甲、轮胎、云台、发射器和机械臂贴图，并转换 Unity 法线通道编码。
  来源见素材文档；该次验证时尚未提交生成物。
- 加入预过滤环境反射、三维地面和透明投影接收器。宽屏场景延伸到整页内容区，地面同时进行
  空间及视口边缘渐隐；深色主题检查未再出现独立矩形底板。
- 桌面真实 GPU 测试 4 项、加载失败重试 1 项、`ConsoleUiTest` 52 项全部通过。
  图像测试等待模型本身的有色像素，避免将“资源就绪”或橙色连接按钮当作 GPU 已出图。
- 重新人工检查浅色桌面、深色平板、360dp 英文大字体、S1 姿态变化、机械臂和基础底盘截图。
  本工作树的桌面分发包重新打开，并通过界面重置按钮核对默认构图与贴图显示。
- Android Debug 构建、lint 和生命周期测试 1 项通过。正常运行的 APK 截图确认官方部件贴图、
  地面、阴影及手机连接操作同时显示；该截图独立于暂停时钟的 instrumentation 测试。
- 生成器 Ruff 格式/检查和 Pyright 通过。三份 GLB 的 Khronos 格式检查均为 0 error；
  基础底盘、S1、EP 分别为 8,607,764 / 14,297,472 / 15,006,768 字节，
  分别有 4 / 7 / 7 个带颜色贴图与法线贴图的材质。
- Validator 分别有 19 / 33 / 50 条 `MESH_PRIMITIVE_GENERATED_TANGENT_SPACE` 提示：
  当前由 Filament 为有法线贴图的 primitive 生成切线空间，尚不保证其他 glTF 查看器逐像素一致。
  桌面 Metal 与 Android 模拟器 OpenGL ES 的画面已检查；未新增实机姿态标定或性能结论。

## 透明材质与实机联动复核

本节补充此前未连接机器的验证，不将前面的离线结果视为实机证据。

- 对照 DJI 装甲套件实物照片，将装甲修正为烟黑透明塑料，并恢复底盘后部中控盖透明度。
  材质参数与近似边界维护在素材文档。当前使用 alpha 混合，不模拟折射。
- 透明版本桌面真实 GPU 4 项测试通过，人工检查默认视角和旋转后的画面；Android Debug 构建通过。
  本轮未重新进行 Android 屏幕像素与实体设备检查。
- 21:28 的实机测试通过：普通首页会话收到 29 个不同接收时间的云台数据，遥控未开启时也持续更新。
  用户已授权平整空地、直径一米内运动。测试仅使用 400 ms 的小幅速度脉冲，每段结束发送零速，未发射。
- yaw 从 −0.2° 到 7.7°，反向后约 −0.6°；pitch 从 −1.1° 到 5.0°，反向后回到 −1.1°。
  使用真实 ConsoleModel/AppSession 与实际 GPU，断言角度变化及模型几何像素变化。
  这是设备角度回传至渲染的闭环证据，不是外部相机或量角器的物理角度标定。
- 底盘前后速度为 ±0.08 m/s，各 400 ms。现有原始字段出现约 0.036 的位移变化，
  但尚未完成坐标与单位标定，模型仍不消费这些字段，不宣称车轮、底盘、IMU 或全部传感器已联动。
- 测试结束零速、退出遥控并关闭会话。随后只读底盘主题探测未能建立 App 会话；
  网络 ping 可达，但 App 握手没有应答，原因尚未确定，不能据此断言机器人关机或协议不支持。
- 真实机器人 UI 测试使用 `HANPPIE_GPU_TESTS=1`、`HANPPIE_TEST_ROBOT_IP` 和 `HANPPIE_TEST_APPID` 显式启用；
  运动另需 `HANPPIE_SCENE_ALLOW_MOTION=1`。配置机器人地址时禁用任务的 up-to-date 结果复用。
  截图和原始遥测只留在忽略的构建报告目录，不提交设备日志。

## 官方 App 对照与完整运动链路复核

本节是当前版本的最终复核；前面的 ROS 网格、alpha 混合、未接入底盘等描述是当时的阶段记录。

- 用户通过远程 ADB 提供正在运行的官方 App。实际观察首页正侧面与后侧面，确认之前存在
  轮毂橙斑、云台 UV 错位、装甲透明面斑块；并非只有光照差异。按用户授权退出官方 App 后，
  Hanppie 成功恢复原生会话，先前握手失败不再出现。
- 转换器改为复用原始 Unity 网格、子网格材质槽和装配 Transform，删除 ROS 网格套用官方
  材质的转换路径。原始贴图与表面细节保留，来源和分发边界见素材文档。
- 对法线、AO、阴影接收、双遍透明和不透明材质进行隔离对比，确认壳体斑块来自透明绘制。
  最终采用薄表面透射并开启 Filament 屏幕空间透射；恢复完整贴图和阴影，不保留临时诊断分支。
  后视图能看到透明中控盖下方的橙色内部部件，装甲保持烟黑色表面。
- 新增独立底盘姿态和 ESC 只读订阅。三条运动数据流分别记录接收时间，1 秒未更新则显示暂停并
  保持最后姿态。使用设备角度驱动四轮与底盘方向，不从控制输入推演运动。
- 22:25 完整实机测试 1 项通过：普通首页收到 33 个不同时间戳的云台数据；云台 yaw/pitch、
  四轮编码器与底盘转向均通过真实 ConsoleModel/AppSession/GPU 链路。
  前进后四轮角度各变化超过 10°，实际几何像素显著变化；底盘小角度右转到约 3.63°。
  所有动作仍为 400 ms 低速脉冲，结束归零并退出遥控、关闭会话，没有发射。
- 最终共享回归 70 项全部通过（包含真实 GPU 4 项、独立底盘/车轮画面变化、过期与断线状态、
  Console UI）；协议核心 61 项中 56 通过、5 项可选测试跳过。
  Android 模拟器生命周期 1 项通过；正常启动 APK 的截图确认透明中控盖、装甲、阴影和手机布局。
  Android Debug 构建、lint 和桌面分发包构建通过。
- 最终三份 GLB 均为 0 格式错误，基础底盘/S1/EP 分别为 9,368,992 / 14,012,788 / 20,553,332 字节。
  分别有 30 / 44 / 54 条运行时生成切线空间的提示。Ruff、Pyright 和 diff 格式检查通过。
- 重新打开本工作树的桌面分发包，普通首页自动连接成功，显示电量 32%、云台更新中、底盘方向实时、
  车轮实时；保持在只读首页供用户检查，未进入操控。

验证仍不涵盖全部传感器：底盘俯仰/横滚、IMU、LED 灯色、机械臂关节尚未驱动模型，
不显示世界位置轨迹。车轮编号依据 SDK 顺序与原始网格装配映射，未以外部观测进行逐轮独立标定；
运动测试不替代全行程角度精度、轮胎接地/滑移与长期性能测量。当前透射系数是视觉近似，
不是实物光学参数测量结果。Windows/Linux 桌面和 Android 实体设备上的 Hanppie 渲染仍未验证。


## 全容器车库场景与横屏修订

本节记录后续视觉修订，前文的竖屏截图和渐隐地面属于较早版本，不代表当前支持范围。
横屏产品规范唯一维护在 [DESIGN.md](../DESIGN.md#布局与自适应)，AGENTS.md 引用该规范。

- 移除场景底部说明、手势提示和重置按钮；姿态/加载状态保留无障碍语义，失败时仍可重试。
- 场景覆盖整个首页内容容器；移除独立首页标题栏、620dp 高度上限和渐隐平面，连接操作叠加在真实三维环境上。
- 官方 LAB 3 静态环境转换得到 935 个几何部件，其中 2 个从同名唯一预制体绑定恢复；缺失网格逐项输出到本地转换日志。
  原始地面、纹理与车库物件已恢复，Unity 烘焙光照、专用反射着色器及缺失合批网格未完整恢复，不能称为像素级完整复刻。
- 红外装甲改为黑色低粗糙度薄层透射塑料，减弱表面法线；扬声器连接线改为黑色绝缘材质。具体参数见素材权威文档。
- Android 请求左右横屏，桌面采用横向最小窗口；短横屏移除导航头像，确保五个导航入口均可见。

验证结果：

| 层级 | 结果 |
| --- | --- |
| 桌面 UI | ConsoleUiTest 52、RobotSceneUiTest 4、SceneRecoveryUiTest 1，合计 57 项通过 |
| 实际 GPU 图像 | 桌面 1040×700、平板横屏 1180×820、手机横屏 800×360（英文 1.3 倍字体）；检查了默认、旋转和低角度车库镜头 |
| Android | Android 17 模拟器横屏生命周期测试 1 项通过，覆盖方向声明、左右横屏、导航与后台恢复；另安装最终 APK 人工查看实际 GPU 输出和完整侧栏 |
| 构建与 lint | Android Debug 与桌面 distributable 通过；Android lint 0 errors、12 warnings，其中固定方向告警与已记录的系统限制相关 |
| 素材校验 | 车库、S1、底盘、EP GLB 均为 0 errors；生成切线空间警告分别为 17、43、30、54 |
| 转换器 | Ruff check/format、Pyright 通过 |
| 当前设备 | 最终桌面程序重新建立普通只读会话，显示在线状态和实际姿态；本次视觉修订没有重新执行机械动作，前文的物理测试不能当作本次新增结果 |

截图在本地 `shared/build/reports/ui/`，最终 Android 截图为
`/tmp/hanppie-3d-research/final-landscape-android.png`；构建记录为
`/tmp/hanppie-3d-research/navigation-final.log`。该轮验证时未提交或推送；实机日志留在本地报告目录。

## 近景物件、材质与双向进入过渡

- 复用原始货架、托盘与纸箱素材，补充机器人附近的展示区。该区域是重新构图，不能声称与官方摆放完全一致。
- 移除装甲贴图上的额外暗色乘数，提高粗糙度以保留烟黑塑料细节；参数唯一维护在素材 README。
- 第一版跨导航搬移视频虽通过组件身份测试，但用户仍观察到首帧、黑屏、绿屏和 HUD 突现，不能以此前测试认定体验完成。
- 改为同容器保留场景、视频和控制层。模式准备先于媒体启动，控制输入在过渡完成后才开放；退出立即停止输入，再淡出 HUD、视频并反向运镜。
- 用户要求只有其确认功能完整后才开始 Android 验证，已加入 AGENTS.md。此前追加 Android 回归因模拟器未连接而未运行；已停止该验证路径，本轮后续只验证桌面。
- 桌面首轮 65 项通过：Console UI 52、生命周期 3、场景 GPU 5、双向过渡 2、媒体 3。
  实机连续截帧确认退出交叉淡化和反向运镜；第二次进入仍捕捉到绿线，继续修复解码恢复，未将该轮判定为通过。

2026-09-23 后续复核：

- 解码诊断确认起始 H.264 流存在 slice header 错误，完整 I 帧可带 `iskey:0`；按帧内图像类型恢复，不能只等 IDR 标记。
  帧判定仅消费 H.264 解码诊断，不能把 rawvideo 封装时间戳警告误当成图像损坏；输出保留滤镜时间基准。
  临时诊断写文件代码已移除，运行时不保存画面或解码日志。
- 最新针对性桌面回归 12 项通过：双向过渡 2、镜头数学 1、实际 GPU 5、媒体 4（含真实 FFmpeg 与损坏参考帧恢复）。
  桌面 distributable 构建、diff 格式检查通过。记录：`/tmp/hanppie-3d-research/roundtrip-verified.log`。
- 最终桌面包连接实机，在同一会话连续进入、退出两次。每次进入连续抓取 65 张截图，检查运镜、交叉淡化、HUD 渐显及视频持续解码阶段；
  所查看的采样画面没有此前的黑屏、绿屏或绿线。退出采样确认视频淡出、反向运镜及首页恢复，改进后的路径避开中控上盖。
  该结论限定于本次桌面/设备/网络条件和采样画面，不代表所有丢包场景或逐帧无缺陷证明。
- 未施加运动或发射输入，结束时停留在只读首页；未验证 Android，未提交或推送。

## 2026-09-23：首页布局与进场时序

- 首页移除侧边/底部导航与脚本运行提示，场景全幅、模型居中、主操作下方居中；工作页面入口位于右上。局部组件采用更紧凑的字号与间距，点击区域保持至少 48dp，其他页面主题不变。
- 桌面默认窗口 1120×658，取消进入控制时自动调整窗口大小。默认展示镜头方位 28°、距离 3.1。
- 视频准备移至运镜之前：等待时保留完整首页构图与按钮反馈，视频就绪后才开始运镜，末段提前融合视频，不在运镜结束后等待流启动。移除白色过渡退出按钮，系统返回仍支持取消。
- 桌面定向验证：ConsoleUiTest 52 项、SceneEntryUiTest 2 项、实际 Filament GPU RobotSceneUiTest 5 项，共 59 项通过；桌面 distributable 构建成功。准备阶段镜头保持不动、隐藏控制不接收输入、解码器跨进出复用及取消/断线释放由测试覆盖。
- 当前桌面构建连接真实机器人完成一次进入与退出；连续采样 65 张进入画面、45 张退出画面，抽查包含主页准备、运镜、场景与视频融合、控制界面以及反向返场。所查样本未见黑屏、绿屏、白色退出按钮或镜头到位后数秒停留，窗口比例保持一致。截图采样不等同于逐帧性能测量，未执行运动或发射。
- Android 构建、安装和运行均未执行，继续等待用户确认功能完整。主页视觉风格仍待用户评审，未推广到其他页面。

### 同日后续：实时视频预热与重叠返场

- 前台已连接首页持续解码视频，进入和返回主页均保留同一解码器；不保存历史片段，也不通过旧录像遮盖等待。仅在用户点击后进入遥控模式。后台、断线、离开首页或脚本可能执行时停止预热。
- 进入时检查最近 500 ms 的有效解码帧，避免用已停更的预热结果放行；未完成预热的冷启动仍可能等待。退出同时驱动 HUD 淡出、视频淡出与反向运镜，画面覆盖后退出遥控；返场后关闭监听并结束录像。
- 状态移至右上，设置改为图标，脚本、操控、对话和诊断放在同一排。桌面 GPU 截图检查包含横屏大字体英文布局。
- 63 项桌面定向测试通过：ConsoleUiTest 52、SceneEntryUiTest 2、RobotSceneUiTest 5、DesktopMediaTest 4；桌面 distributable 构建通过。测试覆盖连接后仅预热、未就绪不运镜、预热后无需新建解码器、退出时镜头与视频重叠、后台释放等契约。
- 真实机器人桌面检查包含冷进入、退出及预热后再次进入。冷进入仍见准备阶段；预热再次进入时解码计数从约 990 持续增长至 1080，未重新启动解码器。点击后 AX 读取完成开始的采样中，第 320 ms 已开始运镜，第 1724 ms 已显示视频；该采样不是精确的点击响应耗时测量。退出采样中已直接出现运动中的返场视角，未见先停在静止空地等待的阶段。
- 未执行运动、发射或 Android 验证。

### 同日后续：首页状态与入口精简

- 首页隐藏信号强度与状态右箭头，连接方式、电量复用本地官方图标；设置仍为独立图标。底部保留脚本、操控、对话，移除诊断及未连接时的连接配置入口。
- 诊断迁至连接状态弹窗机器人标题右侧的页面入口图标；连接配置仍从未连接状态弹窗进入。其他工作页面样式保持原样。
- 场景加载失败时的重试按钮移到中央，避免与精简后的底部主操作重叠。
- 60 项桌面测试通过（Console 52、GPU 场景 5、进出场 2、场景失败恢复 1），桌面构建通过。桌面实际检查官方图标、无信号/箭头的首页，以及弹窗入口进入诊断并返回主页；未验证 Android。

### 提交前回归

- 同步更新其余设计及文件管理测试的首页入口、横屏和全幅场景断言；补齐环境贴图引用的 CC0 原文。
- robot-core 桌面测试 61 项中 56 通过、5 项按运行条件跳过；共享桌面测试 262 项中 256 通过、5 项跳过，1 项聊天气泡宽度断言在整套运行中失败。该断言未修改，单独重跑通过；不将该次整套运行记为全绿。
- 桌面 classes 构建、素材转换脚本语法检查和 diff 空白检查通过。Android 本轮仍未验证。
- 按用户明确要求删除厂商素材禁提规则；本次将当前转换模型和官方图标纳入提交，保留来源记录。提交本身不更改视频预热策略，也不推送。

### 同日后续：限时主页预热与失焦保留控制

- 每次回到已连接的前台首页开启最长 5 秒视频预热，结束后停止取流与解码，仅保留最后画面。离开首页、断线、切换连接或脚本执行时失效；重新进入首页重新采集。
- 5 秒内的缓存画面仅用于衔接运镜与新流启动，操作必须等待最近 500 ms 的实时帧及控制授权；缓存过期则先准备新画面。
- 窗口失焦保留控制界面与视频，仅停止正在持续的动作并禁用输入；恢复焦点后重新授权，不恢复先前按住的操作。
- 定向桌面测试 10 项全部通过：SceneEntryUiTest 3、RemoteLifecycleUiTest 3、DesktopMediaTest 4。覆盖预热超时停流、返回首页刷新、连接变化清缓存、缓存不授权输入、进场中及控制中失焦、恢复不重放输入。桌面 distributable 构建通过；日志 `/tmp/hanppie-3d-research/snapshot-focus-final.log`。
- 最终桌面包连接实机，完成进入与退出控制，视频持续解码；首页空闲时检查无 FFmpeg 子进程。未施加运动或发射。原生工具切换窗口后未取得可靠的持续失焦状态，因此失焦行为以本轮自动化生命周期测试为证据，不声称已完成原生失焦实测。
- Android 本轮未构建、安装或运行。未提交或推送。

### 同日后续：控制界面与连接弹窗精简

- 控制界面与首页共用官方电池图标，移除表情与视频解码帧数；降低浮层不透明度，圆形按钮视觉直径缩至 40dp、点击区域保留 48dp，摇杆向内、向上留出操作空间。
- 操控快捷键隐藏下方档位、摇杆和发射区；点击非交互画面恢复，顶部状态与媒体按钮保持。弹窗调试入口使用页面同款 Activity 图标，标题复用实际产品型号，IP 只在详情出现一次。
- 角度指示器可点击回中。使用已验证的角度反馈和速度通道，不新增协议；停止条件与边界见架构文档。桌面实机从显示约 +66° 的偏航开始，点击后显示 +52°，随后收敛至 0°，视频从斜向上视角回到前方。此次证据为遥测与机载视频，未使用外部量角标定，也未单独记录俯仰数值；未驱动底盘或发射。
- 64 项桌面测试通过：ConsoleUiTest 52、GimbalCenteringTest 2、RemoteLifecycleUiTest 3、SceneEntryUiTest 3、DesktopMediaTest 4。覆盖回中限速、收敛、超时、过期数据、手动取消和触控隐藏恢复等。最后调整按钮绘制以保留点击区域后，重跑小横屏布局和输入方式切换 2 项通过，桌面 distributable 构建通过。日志分别为 `/tmp/hanppie-3d-research/cockpit-refinement.log`、`/tmp/hanppie-3d-research/cockpit-visual-final.log`。
- 最终桌面包实测确认无解码计数，电池与半透明布局正常；按 3 隐藏触控区，点击角度交互控件仍保持隐藏，点击空白视频恢复。连接弹窗显示 RoboMaster S1、正确诊断图标以及单处 IP。测试结束退出控制返回主页。
- Android 未构建、安装或运行。本轮未提交或推送。

### 同日后续：弹药图标、窗口位置与过渡细化

- 弹药切换移至右侧中部，复用官方红外光束、水弹图标并记录来源；短横屏向内避让发射和摇杆。桌面实测来回切换图标与弹药状态，未执行发射。档位条缩至 160dp 宽、28dp 视觉高度，保留 48dp 点击高度；发射默认中性，仅按住时高亮，失焦禁用不改变按钮颜色。
- 修正返场结束被误判为准备阶段导致的首页按钮闪烁；脚本页返回后的冷进入立即开始运镜并并行启动视频，移除等待视频、音频的文字。进入与退出截图的抽查样本中，镜头已在加载期间移动，未见此前等待文字或按钮闪烁；这不是逐帧无缺陷或网络等待归零的证明。
- 原生窗口坐标写入设置并在重新打开时恢复。实际包保存并恢复坐标 (48, 78)；工具拖动未取得窗口移动证据，因此其他位置及多显示器边界由自动化测试覆盖。
- 20 项定向桌面测试通过，覆盖 HUD、窗口位置、设置持久化、进出场、媒体、小横屏布局及输入方式切换。桌面 distributable 构建成功，日志 `/tmp/hanppie-3d-research/controls-entry-final.log`。
- Android 未构建、安装或运行；本轮未提交或推送。

### 同日后续：共享末帧与固定进场

- 最后画面改由会话共享的视频缓存持有，退出页面不会丢弃；同地址重新打开视频复用最后图像，首个新解码帧替换它。删除缓存 5 秒有效期及冷进入减速、超时重试路径，输入仍需新实时帧。当前机制见架构文档。
- 弹药按钮靠近右侧摇杆上方，与发射按钮横向分开。桌面包在脚本页往返后进入操控，连续采样 25 张画面，抽查融合中段及结束画面，确认已有摄像头图像衔接且按钮归入右侧操作区；不声称所有网络条件下零延迟。未施加运动或发射。
- 5 项定向桌面测试及 distributable 构建通过，覆盖固定运镜、缓存不授权操控、跨页共享、不同地址隔离、HUD 样式和小横屏布局。Android 未验证，未提交或推送。

### 同日后续：入场与发射反馈时序

- 遥控授权移到进场运镜期间并行执行；实时画面与授权仍共同限制驾驶输入。水弹反馈改为发射发送回调触发，保留 400 ms 枪口灯周期和原报文顺序。准星消费最新事件以避免动画排队。信号条替换为现有图标库的中性色图标。
- 19 项测试通过：AppSessionIntegrationTest 5、RemoteFireUiTest 10、SceneEntryUiTest 3、RemoteHudUiTest 1。覆盖发射回调早于灯效收尾、单发/持续发射以及进场中授权；桌面 distributable 构建通过，日志 `/tmp/hanppie-feedback-sync.log`。
- 未执行实际水弹发射，未采集机器人音频，不将代码时序测试当作物理音画同步证明；Android 未验证，未提交或推送。

### 同日后续：信号语义、状态条与视频窗口比例

- 普通界面统一信号等级，移除脚本和聊天状态中错误的百分号；原始值进入调试遥测。操控状态条预留字段宽度，夹角数值左对齐并统一图标文字间距。桌面实测显示“强”，角度紧随图标，视频填满内容区。
- 操控窗口按当前视频比例约束内容区，扣除原生边框并在退出时恢复原尺寸。4 项桌面定向测试通过，覆盖信号变化时宽度稳定、小横屏布局、双轴比例换算、标题栏及最小尺寸；桌面构建通过。工具拖拽未实际改变窗口尺寸，因此未将该操作算作原生拖动验证。
- 日志 `/tmp/hanppie-signal-aspect.log`。未施加运动或发射，Android 未验证，未提交或推送。

### 同日后续：图形信号与主页统一比例

- 操控信号移除可见文字及其占位，使用 Lucide Signal / SignalMedium / SignalLow / SignalZero 表示档位；占位恒定 20dp，保留本地化无障碍描述。电量、信号、夹角三组之间统一 16dp，组内图标与数值 6dp。
- 桌面比例约束从主页起持续存在，不再依赖操控状态切换，也不在退场时恢复旧尺寸。实际包检查主页及操控均为 1120×658，视频区域填满，信号不再留出文字空白。未操作运动或发射。
- 4 项针对性桌面测试及构建通过；状态条测试增加无信号文字、图标固定宽度和信号至夹角间距断言。日志 `/tmp/hanppie-compact-hud.log`。Android 未验证，未提交或推送。
