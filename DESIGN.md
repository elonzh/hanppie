# Hanppie 设计规范

## 产品气质

Hanppie 的名字灵感来自电影《超能查派》：让机器逐渐拥有智能，也让人愿意与它相处。品牌头像采用已经确认的正面机械机器人头部：不对称双天线、黑色面罩、蓝灰点阵眼睛、中央镜头、暖白护甲、石墨下颌与少量机械橙。它是软件角色，不替代也不虚构 RoboMaster S1 的实物外观。

选定视觉方案为 Graphite Orange。普通工作页面使用冷中性浅灰背景、白色内容面与石墨文字；机械橙只标记主要操作、当前选择和记录状态，低饱和蓝灰只表达信息与智能体状态。全屏遥控使用同一语义的深色 HUD，不把普通页面染成暖黄，也不使用荧光青。整体清晰、克制，有机械感和亲和力；不叠加装饰性雷达、网格、霓虹发光、拟真磨损或无意义的仪表。

## 组件体系

Graphite Orange 是 miuix 上的品牌样式层，不是新的组件库。导航、按钮、卡片、输入框、开关、弹窗、菜单、进度和文字继续使用 miuix 的组件、字阶、形状、焦点、禁用态及交互反馈，通过统一 `MiuixTheme` 定制颜色；单个页面不得复制绘制一套外观相似的基础控件。只有遥控摇杆、方向指示、瞄准线、点阵表情和品牌头像属于 Hanppie 专用组件。禁止混入 Material Design 风格的导航、按钮、输入框、弹窗及 Markdown 控件。

尺寸 token 以 `HanppieDesignTokens.kt` 为程序实现入口：紧凑断点 `720 dp`、内容最大宽度 `1080 dp`、页面水平边距 `20/28 dp`、卡片圆角 `20 dp`、常用触控目标至少 `48 dp`、遥控摇杆 `132 dp`。页面布局由可用窗口尺寸决定，不能用“手机/平板”设备名称替代实际尺寸判断。

## 固定外观与显示模式

Graphite Orange 是 Hanppie 唯一的软件主题，不提供 miuix 原生、其他配色预设或自定义色板。固定主题用于保持软件图标、品牌头像、点阵表情、普通页面和驾驶舱的一致性，也避免用户配置产生不可验收的对比度与状态语义。历史版本保存的主题名和自定义色值不再生效。

设置页的「显示」分组只提供跟随系统、浅色、深色三种显示模式，默认跟随系统。模式切换即时生效并自动保存，不重建业务状态、不清空编辑器或对话草稿。旧版外观配置升级时只迁移其中的显示模式。

## 查派浅色调色板

| 角色 | 色值 | 用途 |
| --- | --- | --- |
| 冷灰底色 | `#F3F5F7` | 普通页面背景 |
| 内容面 | `#FFFFFF` | 卡片和主要内容容器 |
| 次级面 | `#E9EDF1` | 导航、分组和低层级容器 |
| 石墨 | `#20242B` | 标题与正文 |
| 次文字 | `#5C6670` | 标签与辅助状态 |
| 机械橙 | `#DF6B38` | 主要操作、当前选择、记录提示 |
| 信息蓝灰 | `#688B99` | 智能体与只读信息状态 |
| 边界 | `#B9C1C9` | 输入框和可交互边界 |
| 分隔 | `#D8DDE3` | 卡片及内容分隔 |

## 查派深色调色板

| 角色 | 色值 | 用途 |
| --- | --- | --- |
| 石墨底色 | `#11161C` | 深色页面与遥控底色 |
| 装甲面 | `#1C232B` | 卡片、内容容器和 HUD |
| 抬升装甲面 | `#27313A` | 弹窗、强调容器和触控按钮 |
| 机械橙 | `#E0713C` | 主要操作、摇杆和记录提示 |
| 信息蓝灰 | `#6F929D` | 智能体状态、实时方向指示 |
| 主文字 | `#E7ECF1` | 标题与正文 |
| 次文字 | `#A8B1BA` | 标签与辅助状态 |
| 边界 | `#46515C` | 可交互边界 |
| 分隔 | `#303944` | 容器分隔 |
| 错误 | `#FFB4AB` | 错误与停止动作文字 |
| 错误容器 | `#73322C` | 需要明确后果的错误操作 |

橙色只用于少数重点，不能把所有按钮都刷成橙色。正文保持高对比；小字不叠加低透明度。错误以文字和语义同时表达，不仅依赖颜色。浅色普通页面与深色遥控共享颜色语义，但不强制使用相同明度。

## 页面与导航

一级入口为设备、脚本、诊断、对话、设置。窄屏使用 miuix 底部导航，宽屏使用 miuix NavigationRail。导航头部只可使用小尺寸机器人标记，不使用字母缩写，也不在每个页面重复头像。遥控是设备页进入的独立界面，不占一级导航。连接成功后保留设备概览，由用户进入遥控。

设备概览由四层组成：白色设备连接卡、三列状态卡、必要的网络说明、工作入口。手机连接卡上半部左侧为品牌小字／设备型号／连接地址，右侧为圆形浅灰承托的头像；下半部为两条等宽操作。桌面连接卡将设备身份与 `260 dp` 宽的操作列并排，不能把按钮拉满整个窗口。页面内容居中并限制最大宽度。电量、信号、脚本状态各自采用功能图标、主要读数、辅助标签的层级，未知值显示 `—`。可用宽度除以系统字级倍率后不足 `300 dp` 时，状态卡改为纵向三行，避免标签拆字。工作入口导航到脚本库、诊断和对话；FTP 机内文件属于维护能力，只从诊断页进入，不与正常设备控制并列。

头像在设备连接卡中表达应用伙伴身份，不作为 S1 实物照片；对话空状态居中使用 `112 dp` 头像和一句标题，消息列表不反复添加大头像。脚本库空状态和诊断空状态用同一线性文件图标，前者是紧凑横向说明，后者在记录区居中；不额外添加吉祥物或无意义的装饰插图。

- **设备**：设备名称、连接地址、连接操作、电量、原生信号质量、脚本状态；手动连接才展开 IPv4 与 AppID。连接状态与设备详情分开表达。
- **诊断与内部文件**：诊断页使用 Miuix `TabRow` 在“日志／遥测／报文／FTP”间切换，未连接时 FTP 标签只显示连接提示。文件工具栏依次显示父目录、当前绝对路径、刷新、上传和新建目录，正文明确这里只是 S1 经匿名 FTP 开放的内部维护数据区，不是普通音频库；搜索与较低层级的轮廓式“全部／音频／程序”筛选只作用于当前目录，目录始终保留以免筛选后无法继续导航。文件项显示类型、大小和时间，行尾提供快速打开，选择后在底部集中显示快速打开、下载、重命名和删除；快速打开先下载原始机内数据到应用私有临时目录，再交给系统关联应用，但不承诺识别 S1 加密内容。目录点击进入，尾部菜单选择目录后才能改名或删除。删除必须二次确认且不提供递归删除，同名上传自动生成新名称，不静默覆盖；非 ASCII 上传名先转换为可预测的安全英文名，手工新建和重命名只接受 ASCII。`/python/python_raw.dsp` 标为 Lab 保留槽位，只允许打开或下载。返回、刷新、上传、下载、文件、目录、快速打开、重命名、删除和更多操作统一使用 Lucide 线性图标；上传与下载必须分别使用明确的 `Upload` 和 `Download`，不复用导入／导出语义，也不为常见图标自绘近似形状。窄屏和宽屏使用同一信息层级及至少 `48 dp` 的操作目标，宽屏提供列表滚动条。
- **脚本**：先展示“我的脚本”和“预置脚本”，再进入等宽源码编辑器。窄屏卡片单列，宽屏按行排列；新建、导入、重命名、删除和预置模板都从程序库进入，保存到库与导出 `.py` 是两个独立动作。编辑器使用顶部栏最左侧返回图标回到脚本库，Android 系统返回手势执行相同导航；未修改文档直接返回，只有源码实际变化才确认放弃修改。点击“运行脚本”本身就是执行意图，软件覆盖上传当前源码后立即启动，不再增加第二道复选框。启动后进入独立运行界面：手机竖屏先状态后日志，小屏横屏和宽屏采用状态、日志左右分栏，日志占据主要剩余空间并自动跟随最新输出；返回只离开运行界面，不停止脚本。离开脚本页后，其他一级页面保留可点击的全局运行状态条与停止入口。除“运行脚本”这一主操作和需要明确后果的确认框外，返回、新建、导入、导出、保存、重命名、删除、查看运行和停止使用带无障碍名称的 48dp 图标按钮。预置库固定为 12 个有独立主题与起承转合的节目，区别应体现在动作、节奏或剧情结构上，不能只替换灯色和标题；摘要说明节目内容、运动范围和模拟边界。会移动云台或底盘的预置脚本必须在卡片上直接标识，不能只靠颜色。
- **诊断**：日志、遥测、报文使用 miuix 分段标签分类查看，选中项采用浅橙底与深色文字，清空使用 48 dp 图标按钮。原始字段保留原意，不把未知字段包装成可靠仪表。
- **对话**：Markdown 正文与流式回复、图片、输入框、语音与发送/取消。朗读与新对话采用带无障碍标签的图标按钮。脚本执行审批独立于 Markdown，必须保留明确的批准与拒绝按钮。
- **设置**：显示模式、语言下拉选择（跟随系统、简体中文、English）、操控分组（五档平移/旋转速度、云台灵敏度、摇杆死区、四种遥控 LED 状态色和快捷键）、模型地址/名称/密钥、自动朗读开关与语音服务入口。不存在主题选择和软件配色编辑。语言、显示模式和云台灵敏度使用短下拉菜单；运动参数直接在页面中编辑；LED 状态色通过色块按钮打开 Miuix 色相、饱和度和亮度选择器，实时预览，确认后应用，取消不修改；不提供色值输入或透明度设置，快捷键点击后直接按新键，冲突时交换两个动作的绑定，不使用参数配置弹窗。

## 遥控

触摸以横屏为操作布局；Android 进入遥控时请求横屏，退出恢复原方向设置。系统大屏/多窗口如果不接受方向请求，竖向窗口显示横屏提示并保持待机，横向窗口正常显示控制。

视频占满背景并保持比例，进入驾驶舱时默认开启；用户仍可手动关闭。视频、监听、拍照和录像使用一组固定尺寸的图标按钮，操作名称通过无障碍语义提供。照片与录像直接保存到本地，界面只显示一行保存状态或路径，不能用大块面板遮挡视野。

媒体、返回和发射采用真正的矢量图形，禁止用 `♫`、`◉`、方框或箭头字符代替功能图标。SVG 母版使用 `24×24` 坐标和 `1.75` 线宽，程序按语义统一着色。媒体按钮为 `48 dp` 圆形深色半透明面，带细边界；录像图形使用红色，停止录像使用实心方形。速度选择是连续的深色胶囊，五个触控区各为 `48 dp`，只有当前档位显示橙色圆形底。发射使用橙色准星圆形按钮，弹药名称保留文本且紧邻右侧。

摇杆使用外边界、内行程环、四个方向点和蓝灰拇指帽，外径 `132 dp`；轻微明暗层次用于分辨可拖动部分，不引入霓虹或复杂纹理。底盘和云台名称用 `12 sp` 文字放在下方，避免大字抢占视频。摇杆的位图不可替代真实手势组件，图形随实时输入移动，释放和取消仍归零。

只读连接状态集中在顶部中部，显示电量、原生链路质量和底盘/云台水平夹角；镜头视野固定朝屏幕上方，中央摄像头固定；四轮底盘按相对镜头方向旋转，以单色尖头轮廓标明车头，不以车身颜色暗示方向。方向图统一使用 28 dp，去掉旁边的说明文字。角度区域固定为 40 dp，并使用等宽数字显示底盘相对镜头的带符号方向，紧凑横屏压缩图形，不能与右上媒体按钮重叠。没有质量或角度推送时显示未知，不用收包数量伪装成信号强度。返回位于左上，视频、监听、拍照和录像位于右上。界面不设置急停、停控、遥控启用或发射启用开关；当前弹药与直接发射按钮属于右手操作区，不能放在中央形成遮挡。常用触控目标至少 `48 dp`。

手机横屏中，直径 `132 dp` 的底盘摇杆与云台摇杆分别贴近左下、右下拇指自然落点，距离安全区边缘 `16 dp`；五档速度状态靠近左摇杆，弹药切换、直接发射及云台角速度靠近右摇杆。中央下方不堆叠面板，取景中心只保留低对比瞄准线。桌面端维持同一空间映射，并在实际键盘输入后显示快捷键提示；不能把手机方案放进平板大小的画框来代替真实横屏布局。

设备页仅在连接成功后提供“开始操控”入口，断开或失联时返回设备页。连接后进入全屏遥控就是本次操控授权，界面取得焦点后建立直控通道，不再要求二次启用；只有持续摇杆输入或明确的动作点击才产生控制。释放或取消手势、失焦、返回控制台、离开页面或失联时立即归零并结束当前操控通道；重新取得焦点可以恢复通道，但输入仍从全零开始。底盘使用五档速度并默认三档，触摸端在左摇杆上方直接选 `1–5` 档；键盘数字 `1–5` 直接选档，Q/E 分别控制车头逆时针/顺时针原地转向。`G` 切换弹药，空格发射，`R` 拍照，`Shift+R` 开始或停止录像，按住 `T` 采集对讲、松开后发送到机器人扬声器，`M` 开启或关闭机器人麦克风播放，`Esc` 返回控制台。水弹发射前同时启用枪口开火灯和可见发射器灯，发射后及时关闭。前台失联后显示重连状态并自动恢复连接，停留在驾驶舱时视频随连接恢复；恢复连接本身不得产生机械动作。用户可以通过“断开连接”取消重连。只有实际键盘输入后显示当前配置的快捷键，触摸时隐藏，不根据窗口宽度推断键盘。

## 布局一致性

替换组件库只改变控件样式，不撤销已经确认的导航、横屏、输入与操作布局。首页搜索/手动连接、进入遥控/断开连接分别使用同宽、同高的纵向按钮组，间距 12 dp、触控高度至少 48 dp；主操作用强调色，次操作用中性色。弹窗内的成对确认操作等宽排列。多行对话输入时，语音与发送图标在输入区底部对齐。

## 文案与交互反馈

文案优先给出名称、状态和下一步动作。删除重复介绍、常驻教程和面向实现的耗时信息。明显的图标动作使用无障碍名称；执行、拒绝、连接、保存等需要明确后果的动作保留简短文字。说明仅放在必要的审批、权限与异常场景，或可展开的细节中。

普通控件使用 miuix 的交互反馈与禁用态。视觉状态不能改变已有的机器人安全边界，也不能用成功色宣称未确认的物理动作已完成。

## 品牌素材

完整母版、资源清单和可重复导出脚本位于 `assets/brand/graphite-orange/`。程序消费以下资源：

- Android 使用自适应图标：机械橙背景、保留安全区的彩色头像前景，以及 Android 13 单色轮廓；不使用字母图标。
- macOS、Windows 和 Linux 使用同一张方形头像图标，分别导出为 `icns`、`ico` 和 `png`。
- Compose 页面使用透明头像 `hanppie_companion.png`，用于设备身份、导航品牌和 AI 对话空状态，不另画一个导航品牌形象。
- 通用操作、媒体、状态和一级导航图标统一使用 Compose Icons Lucide，由 `WorkbenchGlyph` 建立唯一语义映射；品牌导出目录不再维护重复的通用 SVG。
- 点阵表情只映射现有 `STANDBY`、`ACTIVE`、`RECORDING`、`TALKING` 四种遥控状态，不创造新的设备能力或状态。普通按钮和导航继续使用功能图标。

所有衍生位图由 `assets/brand/graphite-orange/export.sh` 从母版生成。修改母版后必须重新执行脚本并检查 Android 常见遮罩、macOS Dock/Finder、Windows 安装包和 Linux 启动器效果，不能手工分别维护多套不同头像。

可直接打开 [素材预览](assets/brand/graphite-orange/preview.html) 检查头像、应用图标和四种点阵状态；它是品牌素材展示，不是功能图标目录或虚构设备数据的应用截图。应用图标必须同时覆盖原生安装包、开发启动的 Dock 和窗口图标，不能只检查 `icns` 文件存在。透明资源必须检查真实 alpha 通道，生成图里的棋盘格不算透明。当前可运行平台与 iOS 边界以架构文档为准，不用设计稿冒充平台实现。

## 实现与验收

实际模块、依赖、数据流与安全边界以 [架构文档](docs/architecture.md) 为准，本文件维护唯一的视觉和交互规范；概念图不是实现规格。

验收覆盖中文/英文、手机竖屏主页面、手机横屏驾驶舱和桌面窗口；iOS 复用相同尺寸与安全区规则，不另造一套视觉语言。重点检查固定主题在浅色/深色下的对比度、应用图标遮罩、长文流式渲染、输入草稿保留、下拉菜单、触控热区、键盘提示切换、横竖屏边界与遥控归零。手机横屏必须用真实小屏尺寸检查两侧控制区是否可达、中央画面是否无遮挡；桌面同时检查窗口缩放、键盘焦点和失焦归零。主要文字、次文字和关键操作需通过 WCAG AA 对比度检查。UI 模拟和构建通过不代表实机运动、发射或媒体链路已验证。

桌面普通页面最小尺寸为 `320 × 480`，可缩窄查看移动布局；进入操控时窄窗自动调整为 `1040 × 700`，操控期间最小尺寸 `740 × 480`，退出后恢复原工作窗口尺寸。导航按设备、脚本、对话、诊断、设置排列；侧栏使用居中的 48 dp 纯图标按钮，不保留标签空白，保留无障碍名称。中心准星使用 `24 dp` 中空短线，白色细线带暗色描边，不遮住目标中心。macOS 使用独立的透明留白圆角图标母版 `app-icon-macos.png`；Dock、窗口和 ICNS 共用该母版。

右上连接状态可打开连接管理弹窗，显示状态和地址，并按连接状态提供断开、查找设备或手动连接入口。桌面诊断输出、脚本运行日志和对话历史提供可拖动的垂直滚动条，触摸平台保留原生手势滚动。

设备、对话、诊断、设置与脚本库／编辑器／运行页共用连接状态入口和弹窗；弹窗包含共享头像、型号、地址、电量、信号与脚本状态。对讲按下后先显示准备麦克风，收到并保留首批采样后才显示录音中；准备期间松开或失焦仍取消当前录音状态。
