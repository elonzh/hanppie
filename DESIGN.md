# Hanppie 设计规范

## 文档职责

本文件只维护长期稳定的产品体验原则、视觉语言、交互规范和验收规则，用来帮助设计与实现面对新需求时重新判断，而不是固化某一版界面。

以下内容不得写入本文件：当前类名和模块、依赖版本、端口或报文、存储实现与数量、已连接设备、实测数值、具体型号验证结论、现有页面清单、临时兼容方案和单次测试结果。它们分别属于 [当前实现与能力边界](docs/architecture.md)、[RoboMaster 上游研究](docs/architecture-robomaster.md)、README、日期化记录或 Git 历史。

设计规范描述“为什么”和“必须满足什么”。当前代码只是其中一种实现；当实现与原则冲突时，应重新设计实现，不能把既有界面反写成原则。

## 产品目标与受众

Hanppie 首先应让普通用户和小朋友能够独立完成日常使用，再让家长、教师、创作者和开发者按需进入更深的功能。专业能力是产品的一部分，但不能成为所有人的认知负担。

设计决策按以下顺序权衡：

1. 让用户先看懂当前状态和下一步。
2. 通过安全、可靠的自动化减少选择、输入和记忆。
3. 在失败时提供可恢复的路径，不迫使用户理解内部实现。
4. 让专业能力可发现、可进入，但不干扰日常路径。
5. 在不同窗口、设备和输入方式上维持同一个任务模型。

面向儿童不等于幼稚化。界面应友好、直接、可信，不使用会掩盖风险或夸大能力的玩具化表达。

## 核心体验原则

### 自动化优先

- 软件能够可靠判断、记住、恢复或重试的事情，不应反复交给用户选择。
- 内部发现记录、候选对象和历史状态可以支持自动化，但不能未经设计就变成首页列表。
- 自动化必须可解释：进行中说明正在做什么，失败时说明用户能采取什么行动。
- 自动恢复只能恢复安全的连接和界面状态，不能自动恢复移动、发射、脚本执行、录制或媒体传输等有现实影响的动作。
- 用户明确停止、断开或取消后，系统必须尊重该意图，不能立即以“智能”为由重新开始。

### 渐进披露

信息和能力分为三个层级：

1. 日常层：当前对象、关键状态和一个最常用的下一步。
2. 配置与恢复层：首次使用、添加或更换对象、权限、网络配置和故障恢复。
3. 专业层：地址、标识符、原始数据、日志、报文、内部文件和手动覆盖。

默认界面只显示完成当前任务所需的最低层级。更深层能力应放在语义明确的位置，由用户主动进入。不能因为某项能力已经实现，就在多个页面重复提供入口。

### 状态替换，而非状态堆叠

- 开始处理后，用进度和状态替换已经失效的按钮、候选列表和表单。
- 成功后只保留结果及合理的下一步，不继续展示用于建立该结果的临时选择。
- 状态失效或连接丢失后，及时清除陈旧数据，不能让旧结果看起来仍然有效。
- 失败时只提供与原因相关的恢复动作，不把所有可能操作同时抛给用户。
- 禁用态只用于说明一个仍然相关但暂不可用的动作，不能用来陈列已经没有意义的旧动作。

### 一个页面，一个主要意图

每个页面或主要区域都应有清晰的首要任务和一个主要动作。只有当用户确实需要比较、选择或管理多个对象时才使用列表；协议发现的候选项、内部缓存和历史连接不是天然的用户列表。

## 信息架构规范

### 普通用户路径

- 使用用户语言描述任务和结果，不要求理解地址、标识符、端口、报文、内部路径或类名。
- 首次使用可以提供必要引导；日常使用应直接恢复到可工作的状态。
- 恢复路径优先给出最可能、风险最低且最容易验证的动作。
- 状态页不能同时承担配置页、设备管理页和诊断页的职责。

### 专业用户路径

- 诊断信息按目的分组，并说明数据含义、时间范围和操作后果。
- 手动控制和原始数据必须可访问，但不能成为普通流程失败后的杂物堆。
- 专业入口不能破坏普通用户路径的默认安全性和可理解性。

### 导航

- 导航按用户任务组织，不按模块、协议或代码结构组织。
- 只有高频、独立且需要持续返回的任务才值得占用一级导航。
- 沉浸式或高风险任务可以暂时隐藏常规导航，但必须保留一致、可预测的退出方式。
- 系统返回、页面返回和窗口导航应得到一致结果，不能依赖用户猜测当前层级。

## 连接体验规范

连接是使用产品的基础状态，不应被设计成每次启动都要完成的一项配置任务。

- 在安全且未被用户明确停止的前提下，软件应自动恢复已知配置并寻找可用对象。
- 系统可以在内部维护多个发现候选，但普通界面只呈现当前对象和当前连接状态。
- 连接中只展示必要的进度、解释和取消能力；连接成功后展示“已准备好”和日常下一步。
- 添加、更换对象或修改网络属于明确的配置任务，应与日常自动连接分开。
- 无法可靠判断目标时，不能随机连接，也不能直接抛出原始候选列表；应提供用户能够理解的消歧信息。
- 手动地址和标识符输入属于诊断或高级配置，不应作为默认连接入口。
- 连接建立本身不能触发机械动作。
- 技术细节只在用户主动查看详情或诊断时出现；任何界面、日志和报告都不得泄露凭据。

## 安全与信任

- 界面必须明确区分软件状态、协议响应、设备在线和真实物理动作，不能用较低层级的证据暗示更高层级已经成立。
- 移动、发射、覆盖、删除、录制和向外发送内容之前，应让用户理解动作及其影响。
- 连接恢复、页面恢复或应用重启都不能自动恢复有机械或外部影响的动作。
- 儿童默认路径不暴露凭据、技术参数或开发者动作；仅增加“高级”标签不足以构成安全隔离。
- 错误提示必须诚实说明未知和未验证边界，不能用笼统的成功文案掩盖不确定性。

## 视觉语言

Hanppie 的视觉气质是克制、可靠的机械伙伴：有温度，但不装饰成游戏控制台。

- 品牌风格采用 Graphite Orange：以石墨灰建立结构，以机械橙表达重点和动作。
- 避免装饰性雷达、网格、霓虹光、假仪表和没有信息价值的动态效果。
- 色彩首先表达层级和语义，其次才承担品牌装饰。
- 浅色与深色主题必须分别设计，不能只依靠反色或透明度推导。

### 浅色主题

| 角色 | 颜色 |
| --- | --- |
| 应用背景 | `#F3F5F7` |
| 主要表面 | `#FFFFFF` |
| 次级表面 | `#E9EDF1` |
| 主文本 | `#20242B` |
| 次文本 | `#5C6670` |
| 品牌重点 | `#DF6B38` |
| 辅助重点 | `#688B99` |
| 轮廓 | `#B9C1C9` |
| 分隔 | `#D8DDE3` |

### 深色主题

| 角色 | 颜色 |
| --- | --- |
| 应用背景 | `#11161C` |
| 主要表面 | `#1C232B` |
| 次级表面 | `#27313A` |
| 品牌重点 | `#E0713C` |
| 辅助重点 | `#6F929D` |
| 主文本 | `#E7ECF1` |
| 次文本 | `#A8B1BA` |
| 轮廓 | `#46515C` |
| 分隔 | `#303944` |
| 错误文本 | `#FFB4AB` |
| 错误容器 | `#73322C` |

正文与背景的对比度应满足 WCAG AA。小字号文本不能只靠降低透明度形成层级；错误、警告和状态不能只用颜色表达。

## 组件与图标

- Graphite Orange 是建立在 Miuix 组件之上的品牌层，不重新维护一套基础控件。
- 组件变体来自真实、稳定的交互差异，不能只为视觉排列增加平行实现。
- 图标使用维护良好的标准图标库，同一语义在全局保持一致。
- 上传、下载、打开、删除、停止等方向和后果必须准确，不能使用字符、Emoji 或自绘近似符号代替。
- 仅图标按钮必须提供无障碍名称；面向儿童或低频路径时，重要动作同时保留文字。

## 布局与自适应

产品仅支持横屏，所有页面、弹窗、导航、输入和操控流程均以横向空间为设计基线，不再设计或维护竖屏布局。Android 使用横屏方向，允许左右横屏翻转；桌面默认采用横向窗口。

布局仍根据横屏下的实际可用宽度与高度、安全区域、字体大小和输入方式变化，不根据“手机”“平板”“桌面”标签硬编码体验。小尺寸横屏尤其要检查有限高度和软键盘占用，不能用竖屏布局作为回退方案。系统强制的窗口尺寸不等同于产品支持新的屏幕方向。

- 触控目标不小于 48dp，并为相邻危险动作保留足够间距。
- 正文和表单保持可读宽度；宽窗口通过信息分组、侧栏或辅助区域利用空间，不能无限拉伸内容。
- 紧凑空间优先单列和拇指可达；宽空间可以使用侧栏、多列或 supporting pane，但任务顺序和语义不变。
- 大字体下允许重新换行和重排，不能截断主要动作、状态或恢复信息。
- 触摸、鼠标、键盘和系统返回应得到相同的业务结果与安全约束。
- 沉浸式操控应优先保护实时画面、方向控制和拇指操作区，避免常规界面遮挡。
- 尺寸、间距、圆角、内容宽度和窗口断点应集中管理。新增断点必须有内容约束证据，不能只为了适配某一台测试设备。

### 首页场景布局

- 三维场景铺满首页，机器人居中展示；首页不使用侧边或底部导航条。
- 主操作与脚本、对话入口放在画面下方同一排，名称位于左上；连接方式与电量位于右上，不显示信号强度或连接状态右箭头，设置使用带无障碍名称的独立图标按钮。
- 首页不放诊断或“添加或更换机器人”入口；诊断入口位于连接状态弹窗的机器人标题右侧，连接配置也从该弹窗进入。
- 首页不展示脚本运行状态、常驻说明或场景重置按钮。脚本运行反馈留在工作页面。
- 首页按钮采用紧凑的局部样式，触控区域仍至少 48dp；不通过全局缩放或修改框架主题影响其他页面。
- 已连接的前台首页预热实时视频，但不提前开启遥控、监听或录像；进入时衔接最新有效画面，不回放旧视频。视频就绪后才运镜，运镜末段与视频融合重叠。退出时视频淡出与返场运镜同时开始，不先暴露静止的空场景。
- 此轮仅确定首页风格，其他页面布局和样式需由用户明确要求后再推进。

## 文案与反馈

- 状态文案按“发生了什么—是否需要用户行动—下一步是什么”的顺序表达。
- 按钮使用用户意图命名，例如“添加机器人”“再试一次”，不使用内部实现名称。
- 错误应区分自动恢复、用户检查、权限处理和进入诊断，不能只显示“失败”后给出多个相互竞争的按钮。
- 需要等待的操作必须及时反馈进度；成功结果已在页面中明显呈现时，不重复用弹窗或提示打断用户。
- 文案不得声称未被当前证据证明的设备能力或物理结果。

## 领域交互规范

### 操控

- 操控是沉浸式、高风险任务，画面和安全状态优先于装饰与常规导航。
- 触摸和键盘使用同一空间语义；快捷键仅在键盘输入环境中提示。
- 移动、发射和媒体控制是不同风险域，应明确分离。
- 离开页面、失联、应用失焦或控制中断时，必须进入可预期的安全状态。

### 脚本与创作

- 基本任务顺序是寻找或创建、编辑、运行并观察结果。
- 保存、导出和运行语义不同，不能因界面紧凑而合并。
- 运行状态在跨页面后仍然可见，取消必须明确作用于当前运行。
- 示例应在目标或效果上真正不同，而不是只改名字。
- 涉及移动或发射的脚本应直接说明现实影响。

### 对话

- 消息历史和输入是主体，工具调用、授权和执行结果应在视觉与语义上分开。
- 模型生成内容不能伪装成设备已经执行的结果。
- 语音是辅助入口，不能默认持续监听，也不能替代清晰的文本确认和权限反馈。

### 诊断与内部数据

- 每项诊断数据都应说明证据来源、时间和含义。
- 日志、遥测、报文、内部文件和手动覆盖按目的分组，不能混成一个数据列表。
- 方向性操作和危险操作必须使用准确图标、文字和后果说明。
- 原始内容在窄窗口与宽窗口都应可滚动、可阅读，不遮挡主要退出和恢复操作。
- 协议成功不能被描述为物理动作成功。

### 设置

- 设置只承载持久偏好和低频配置，不收纳临时动作或当前状态。
- 名称应让普通用户能够预测改变后的结果。
- 可以安全推断的选项由软件自动处理，不要求用户每次重复选择。
- 重置必须说明范围，并对不可逆或影响较大的结果进行确认。

## 品牌素材规范

- 品牌素材保留单一母版和可复现的导出流程，避免手工维护多个相互漂移的副本。
- 图标需要分别验证系统遮罩、启动器、Dock、Finder 和窗口等真实使用环境。
- 透明区域必须是真实 Alpha，不能用背景色伪装透明。
- 不使用通用占位图标、虚构设备外观或假应用截图冒充真实产品素材。
- 新素材必须有独立的信息或品牌职责；移除后没有实质影响的装饰不应引入。

## 设计实现闭环

设计原则必须由代码和测试执行，不能依赖开发者记忆：

- 颜色、字阶、通用尺寸、图标、导航和品牌素材各有唯一来源；页面不得复制品牌定义或用多个列表的下标维持同一语义。
- 重要流程使用互斥的展示状态。每个状态只呈现当前事实和有效动作，离开时清除陈旧控件与数据。
- 只为跨页面复用且具有稳定语义的内容建立 token 或共享组件；局部布局不做形式化封装。
- 用户文案进入本地化资源；触控尺寸、无障碍语义和危险态由通用控件统一保证；平台层不复制共享界面与业务规则。
- 源码检查阻止新增原始品牌色、绕过图标映射、混用组件体系、硬编码用户文案和重复导航定义。
- UI 测试同时断言应出现和不应出现的内容；视觉回归使用人工确认的基准图，常规测试不得自动重录。
- 测试按风险覆盖状态、语言、主题、窗口、字体和输入方式；必要例外必须限定范围并说明移除条件。
- 改动先更新唯一来源，再迁移完整影响面并删除旧路径；交付必须通过自动化门禁和人工视觉检查，且不得把界面结果当作实机证明。

## 设计评审与验收

每次设计或 UI 改动都应先回答：

1. 这个界面首先服务谁？是否把开发者决策转嫁给普通用户或儿童？
2. 哪些选择、输入和状态可以由软件安全地自动处理？
3. 状态变化后，哪些旧控件、列表和数据应该消失？
4. 专业信息是否只在用户主动进入的上下文中出现？
5. 失败时是否只有一个与原因相关、可执行的下一步？
6. 是否出现重复入口、重复解释或把内部数据直接当成产品界面？
7. 是否正确表达安全、隐私、凭据和物理影响？
