# Hanppie 设计规范

## 产品气质

Hanppie 的名字灵感来自电影《超能查派》：让机器逐渐拥有智能，也让人愿意与它相处。查派与 RoboMaster S1 相近的机头轮廓、蓝黑装甲和橙色零件构成视觉依据。

默认使用 miuix 原生外观。查派预设以蓝黑色承托内容，以橙色标记行动，以少量冰蓝表现智能体。采用 miuix 的组件、字阶、形状和交互反馈，通过统一 MiuixTheme 建立辨识度。禁止使用 Material Design 风格组件，包括导航、按钮、输入框、弹窗及 Markdown 中的控件。整体清晰、克制，有机械感和亲和力；不叠加装饰性雷达、网格、霓虹发光、拟真磨损或无意义的仪表。

## 主题与夜间模式

设置页直接展示「外观」分组，提供 miuix 原生、查派、海蓝、森林、自定义五种选择。默认 miuix 原生，不覆写框架的原生浅色和深色色表。所有主题只改变配色，不切换组件体系。

夜间模式独立提供跟随系统、浅色、深色，默认跟随系统。主题和模式切换即时生效、自动保存，不清空页面内容。查派、海蓝和森林均有浅色与深色配色。

选中自定义后，配色编辑区直接在当前设置页展开，与页面共同滚动，不打开配置弹窗或嵌套滚动区。主题、夜间模式和语言只使用短下拉菜单选值，不进入弹窗表单。

自定义可以分别编辑浅色/深色模式的主色和背景色，支持 `#RRGGBB` 输入与色块预览。文字对比色及容器层次自动计算，只有合法颜色才能应用；还原修改恢复已保存配色，切到其他主题不会丢弃自定义颜色。

## 查派深色调色板

| 角色 | 色值 | 用途 |
| --- | --- | --- |
| 蓝黑底色 | `#11151D` | 页面与遥控底色 |
| 装甲蓝 | `#1B2230` | 卡片、内容容器 |
| 抬升装甲蓝 | `#29334A` | 弹窗、强调容器 |
| 钢蓝 | `#344159` | 次级选中面、用户消息 |
| 机械橙 | `#ED7B43` | 主要操作、启用状态、摇杆 |
| 橙色容器 | `#493025` | 低饱和橙色辅助面 |
| 橙色容器文字 | `#FFB58A` | 橙色容器上的内容 |
| 冰蓝 | `#83D5F3` | 助手状态、实时方向指示 |
| 主文字 | `#E6EBF2` | 标题与正文 |
| 次文字 | `#A9B3C4` | 标签与辅助状态 |
| 边界 | `#46536C` | 容器分隔 |
| 强边界 | `#7C8BA5` | 输入框、可交互边界 |
| 错误 | `#FFB4AB` | 错误与停止动作文字 |
| 错误容器 | `#73322C` | 遥控停止按钮 |

橙色只用于少数重点，不能把所有按钮都刷成橙色。正文使用高对比浅灰；小字不叠加低透明度。错误以文字和语义同时表达，不仅依赖颜色。这些色值仅适用于查派深色预设。

## 页面与导航

一级入口为设备、脚本、诊断、对话、设置。窄屏使用 miuix 底部导航，宽屏使用 miuix NavigationRail。遥控是设备页进入的独立界面，不占一级导航。连接成功后保留设备概览，由用户进入遥控。

- **设备**：设备名称、连接地址、连接操作、电量、原生信号质量、接收帧；手动连接才展开 IPv4 与 AppID。连接状态与设备详情分开表达。
- **脚本**：文件名与未保存标记、打开与保存、等宽源码编辑区、上传/执行/停止。执行授权保留显式开关，风险说明按需展开。
- **诊断**：日志、遥测、报文分类查看，原始字段保留原意，不把未知字段包装成可靠仪表。
- **对话**：Markdown 正文与流式回复、图片、输入框、语音与发送/取消。朗读与新对话采用带无障碍标签的图标按钮。脚本执行审批独立于 Markdown，必须保留明确的批准与拒绝按钮。
- **设置**：外观分组（主题、夜间模式及页内自定义配色），语言下拉选择（跟随系统、简体中文、English），操控分组（五档平移/旋转速度、云台灵敏度、慢行倍率、摇杆死区和快捷键），模型地址/名称/密钥，自动朗读开关与语音服务入口。语言和云台灵敏度不使用单选列表或一组并排按钮。运动参数直接在页面中编辑；快捷键点击后直接按新键，冲突时交换两个动作的绑定，不使用配置弹窗。

## 遥控

触摸以横屏为操作布局；Android 进入遥控时请求横屏，退出恢复原方向设置。系统大屏/多窗口如果不接受方向请求，竖向窗口显示横屏提示并保持待机，横向窗口正常显示控制。

视频占满背景并保持比例，进入驾驶舱时默认开启；用户仍可手动关闭。视频、监听、拍照和录像使用一组固定尺寸的图标按钮，操作名称通过无障碍语义提供。照片与录像直接保存到本地，界面只显示一行保存状态或路径，不能用大块面板遮挡视野。

只读连接状态集中在顶部，显示电量、原生链路质量，以及俯视的底盘轮廓、云台朝向和两者水平夹角；没有质量或角度推送时显示未知，不用收包数量伪装成信号强度。圆形底盘与云台摇杆位于下方两侧，中间保留视野。停止按钮位于右上角且始终可见，与启用和发射分离。档位、弹药和启用控件位于下方中部，发射仅在遥控启用时可用。云台摇杆旁显示当前角速度，设置提供 `15/30/45/60/90/120°/s`，默认 `90°/s`。常用触控目标至少 48 dp。

默认待机，不因连接或进入页面自动启用。按下摇杆才产生控制；释放、取消手势、失焦、离开页面或失联时归零。底盘使用五档速度并默认三档；键盘数字 `1–5` 直接选档，Q/E 分别控制车头逆时针/顺时针原地转向，Shift 同时降低平移和旋转速度。`G` 切换弹药，`R` 拍照，`Shift+R` 开始或停止录像，按住 `T` 采集对讲、松开后发送到机器人扬声器，`M` 开启或关闭机器人麦克风播放。水弹发射前同时启用枪口开火灯和可见发射器灯，发射后及时关闭。前台失联后显示重连状态并自动恢复连接，恢复后仍须显式启用遥控；停留在驾驶舱时视频随连接恢复。用户可以通过“断开 / 清理会话”取消重连。只有实际键盘输入后显示当前配置的快捷键，触摸时隐藏，不根据窗口宽度推断键盘。

## 布局一致性

替换组件库只改变控件样式，不撤销已经确认的导航、横屏、输入与操作布局。首页搜索/手动连接、进入遥控/断开连接分别使用同宽、同高的纵向按钮组，间距 12 dp、触控高度至少 48 dp；主操作用强调色，次操作用中性色。弹窗内的成对确认操作等宽排列。多行对话输入时，语音与发送图标在输入区底部对齐。

## 文案与交互反馈

文案优先给出名称、状态和下一步动作。删除重复介绍、常驻教程和面向实现的耗时信息。明显的图标动作使用无障碍名称；执行、拒绝、连接、保存等需要明确后果的动作保留简短文字。说明仅放在必要的审批、权限与异常场景，或可展开的细节中。

普通控件使用 miuix 的交互反馈与禁用态。主题不能改变已有的机器人安全边界，也不能用成功色宣称未确认的物理动作已完成。

## 实现与验收

实际模块、依赖、数据流与安全边界以 [架构文档](docs/architecture.md) 为准，本文件维护视觉和交互规范。

验收覆盖中文/英文、手机竖屏主页面、手机横屏遥控及桌面窗口；检查长文流式渲染、输入草稿保留、语言下拉、键盘提示切换与遥控归零。UI 模拟和构建通过不代表实机运动或媒体链路已验证。
